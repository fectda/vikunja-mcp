# Archive Report: tasks-all-endpoint-deprecated

## SDD Artifacts

All artifacts for this change have been tracked and persisted:

- **Proposal**: observation #777 (`sdd/tasks-all-endpoint-deprecated/proposal`)
- **Spec**: observation #778 (`sdd/tasks-all-endpoint-deprecated/spec`)
- **Design**: observation #779 (`sdd/tasks-all-endpoint-deprecated/design`)
- **Tasks**: observation #780 (`sdd/tasks-all-endpoint-deprecated/tasks`)
- **Apply Progress**: observation #781 (`sdd/tasks-all-endpoint-deprecated/apply-progress`)
- **Verify Report**: observation #782 (`sdd/tasks-all-endpoint-deprecated/verify-report`)

## Source of Truth

The delta spec for `task-listing` was merged into the main specs. Since no main spec existed, it was copied directly to:

- `openspec/specs/task-listing/spec.md`

## Archive Location

The active change folder has been successfully archived to:

- `openspec/changes/archive/2026-05-21-tasks-all-endpoint-deprecated/`

The SDD cycle for this change is fully complete.
